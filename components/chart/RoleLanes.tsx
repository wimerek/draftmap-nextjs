"use client";
/**
 * components/chart/RoleLanes.tsx
 * Nothing new to render — role sub-column dividers and headers are already
 * handled by PositionColumns. This component is a no-op placeholder kept for
 * future use (e.g. per-lane background tints at higher zoom levels).
 */
export default function RoleLanes() {
  return <g />;
}
